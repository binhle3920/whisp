# Credentials

Whisp stores file-based credentials in this directory. Its contents are ignored by Git
except for this README.

The Gmail input expects:

- `google_credentials.json` — OAuth desktop client downloaded from Google Cloud
- `google_token.json` — generated by `uv run whisp authorize`

Never commit either credential file.
